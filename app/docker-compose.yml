version: '3.8'

services:
  music-recommendation:
    image: music-recommendation-system:latest
    ports:
      - "6969:6969"
    environment:
      - HOST=0.0.0.0
      - PORT=6969
    restart: unless-stopped
